package server

import "testing"

func TestRequestStreamFps(t *testing.T) {
	t.Run("0 fps: receive one frame then no more updates", func(t *testing.T) {
		t.Skip()
	})

	t.Run("10 fps: receive 10 frames per second", func(t *testing.T) {
		t.Skip()
	})

	t.Run("60 fps: receive 60 frames per second", func(t *testing.T) {
		t.Skip()
	})

	t.Run("can stop and start again", func(t *testing.T) {
		t.Skip()
	})
}
